def compute(data) :
	print('called compute function in compute_metrics.py')
	print(data)

def data_size():
	print('Computes: number of echo requests sent, number of echo requests recieved, number of echo replies sent, and number of echo replies recieved')
	print('total echo reqeuest bytes sent, total echo request bytes recieved, total echo request data sent, total echo request data recieved')

def time_metrics():
	print('Average ping round trip time (RTT), echo request throughput, echo request goodput, average reply delay')

def distance_metric():
	print('Average number of hops per request')

